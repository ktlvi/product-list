const Header = () => {
    return (
        <header className="header">
            <div className="container">
                <h1>Product List</h1>
            </div>
        </header>
    );
};

export default Header;
